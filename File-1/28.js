// use const for creating arrays instead



const fruits=['apple','banana','cherry'];//["apple","banana","cherry"] is asinedf some addres for ex 111
fruits.push("orange"); // so in the sasme addres we just push the orange we have no change the adrdress of [apple,banana,cherry] and
//so in const qwe can use array method